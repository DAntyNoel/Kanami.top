{
  "name": "kanami-local-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kanami local file mapping server template for local-server.kanami.top.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/config.js && node --check src/static.js && node --check src/server.js"
  },
  "engines": {
    "node": ">=18.18"
  }
}
