部署到 kanami.top 的后台控制 By DAntyNoel

为了香奈美应援而创建的网站，集合了许多有关香奈美的内容。面向用户的展示必需以香奈美为口吻。

## 公网安全规则

- `local-server` 的 `/files/` 映射不能默认把整个本地目录暴露到公网；生产环境必须使用访问开关、明确白名单、签名 URL 或等价的最小公开目录策略。
- 调试、刷新、缓存清理等维护端点不能公网裸露；`/__reload/state`、`/__reload/trigger`、`/__reload` 这类端点只允许本机访问或使用管理口令。
- 公网 `/health` 只能返回 `ok/status` 等最小健康状态；本地路径、图库根目录、远程目标、模型、provider、本地代理 host/port 等细节必须放到本机或管理接口。

## 前台展示规则

- 首页和面向用户的按钮、标题、说明文案使用香奈美第一人称中文表达，避免英文占位文案和泛用的 `Play/Open`。
- 首页首屏必须有明确的香奈美主视觉，并把主要 CTA 清晰分成资源舞台、聊天入口和小游戏入口。
- `CLIProxy API`、`Usage Keeper` 等后台服务入口不得和普通玩家内容并列高亮展示；应放入后台入口、折叠区或管理页，并降低视觉优先级。
- 表情包和素材展示优先使用本地 WIKI 资源映射数据驱动，避免固定写死少量图片；需要提供“换一批”和前往资源映射的联动入口。

## 仓库管理规则

完成代码更改后，将当前更改的文件按照改动逻辑进行commit，并用中文给出简短的message，随后push。
